<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Storage permissions for file downloads and uploads -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    
    <!-- For Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    
    <!-- Permission to install APK updates -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".CampusVaultApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusVault"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <!-- Splash Activity -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.CampusVault.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding Activity -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:screenOrientation="portrait"
            android:exported="false" />

        <!-- Auth Activity -->
        <activity
            android:name=".ui.auth.AuthActivity"
            android:theme="@style/Theme.CampusVault.NoActionBar"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />

        <!-- Main Activity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/Theme.CampusVault.NoActionBar"
            android:screenOrientation="portrait"
            android:exported="false" />

        <!-- Course Unit Detail Activity -->
        <activity
            android:name=".ui.main.resources.CourseUnitDetailActivity"
            android:theme="@style/Theme.CampusVault.NoActionBar"
            android:screenOrientation="portrait"
            android:exported="false" />

        <!-- Faculty Detail Activity -->
        <activity
            android:name=".ui.main.explore.FacultyDetailActivity"
            android:theme="@style/Theme.CampusVault.NoActionBar"
            android:screenOrientation="portrait"
            android:exported="false" />

        <!-- Program Detail Activity -->
        <activity
            android:name=".ui.main.explore.ProgramDetailActivity"
            android:theme="@style/Theme.CampusVault.NoActionBar"
            android:screenOrientation="portrait"
            android:exported="false" />

        <!-- Resource Detail Activity -->
        <activity
            android:name=".ui.main.resources.ResourceDetailActivity"
            android:theme="@style/Theme.CampusVault.NoActionBar"
            android:screenOrientation="portrait"
            android:exported="false" />

        <!-- PDF Preview Activity -->
        <activity
            android:name=".ui.main.resources.PdfPreviewActivity"
            android:theme="@style/Theme.CampusVault.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".ui.main.settings.SettingsActivity"
            android:theme="@style/Theme.CampusVault.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".ui.main.profile.resources.MyResourcesActivity"
            android:theme="@style/Theme.CampusVault.NoActionBar"
            android:exported="false" />

        <!-- FileProvider for APK installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>