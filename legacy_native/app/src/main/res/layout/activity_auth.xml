<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/main_background_gradient">

    <!-- Animated Blob Background Layer -->
    <View
        android:id="@+id/blobBackground"
        android:layout_width="0dp"
        android:layout_height="400dp"
        android:background="@drawable/animated_blob_shape"
        android:alpha="0.6"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Top Image Section with Parallax Effect -->
    <ImageView
        android:id="@+id/ivBackground"
        android:layout_width="0dp"
        android:layout_height="380dp"
        android:scaleType="centerCrop"
        android:src="@drawable/auth_image"
        android:contentDescription="Background"
        android:elevation="2dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Modern Gradient Overlay with Glass Effect -->
    <View
        android:layout_width="0dp"
        android:layout_height="380dp"
        android:background="@drawable/glass_gradient_overlay"
        android:elevation="3dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Floating Particles Overlay -->
    <View
        android:id="@+id/particlesOverlay"
        android:layout_width="0dp"
        android:layout_height="380dp"
        android:background="@drawable/particles_overlay"
        android:alpha="0.3"
        android:elevation="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Back/Navigation Button with Glassmorphism -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/backButtonCard"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_margin="20dp"
        android:visibility="gone"
        android:elevation="8dp"
        app:cardBackgroundColor="#40FFFFFF"
        app:cardCornerRadius="28dp"
        app:strokeWidth="1dp"
        app:strokeColor="#60FFFFFF"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_arrow_back"
            android:tint="#FFFFFF"
            android:contentDescription="Back"
            android:padding="14dp" />

    </com.google.android.material.card.MaterialCardView>

    <!-- ViewPager2 for Login/Register with Modern Transition -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:elevation="5dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Bottom Brand Glow Effect -->
    <View
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:background="@drawable/glow_circle"
        android:alpha="0.15"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="-100dp"
        android:layout_marginBottom="-100dp" />

</androidx.constraintlayout.widget.ConstraintLayout>